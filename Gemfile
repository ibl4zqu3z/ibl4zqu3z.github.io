source "https://rubygems.org"

gem "jekyll", "~> 4.4"

group :jekyll_plugins do
  gem "jekyll-sitemap"
end

platforms :windows, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end

# `wdm` is optional on Windows.
# Add it only if auto-regeneration gives you trouble, and use 0.2.0:
# gem "wdm", "~> 0.2.0", install_if: Gem.win_platform?
